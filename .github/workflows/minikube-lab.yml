name: Minikube Lab Guide

on:
  workflow_dispatch:

jobs:
  explain-delivery-path:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show learner guidance
        run: |
          echo "This workflow is intentionally lightweight."
          echo "Use CI for tests and build validation."
          echo "Run Minikube deployment labs locally from docs/modules/05-kubernetes-rollout-operations.md."
