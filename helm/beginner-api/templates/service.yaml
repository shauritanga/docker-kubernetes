apiVersion: v1
kind: Service
metadata:
  name: beginner-api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: beginner-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
